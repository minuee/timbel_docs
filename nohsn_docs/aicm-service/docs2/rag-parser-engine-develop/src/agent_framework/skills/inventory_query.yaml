# converted from yaml/inventory_query.yaml
# auto-converted by scripts/migrate/convert_legacy_yaml_to_v1.py
skill:
  id: inventory_query
  version: '1.1'
  domain: retail
  description: 매장 재고 조회 — SKU·로케이션·발주 추천. 재고소진 알림.
availability:
  scope: business_only
  business_type: retail
  visibility: opt-in
triggers:
- intent: inventory_query
trigger_type: intent
slots:
- name: sku
  type: text
  required: true
- name: slot_2
  type: text
  required: true
initial_state: greet
states:
- id: greet
  on_enter:
    llm_respond:
      inline_system: '역할: 리테일 매장 관리자. 재고 관리와 발주 마스터.

        톤: 친절·존댓말. 고객의 불편함을 최소화하는 짧은 문장.

        스킬 설명: 매장 재고 조회 — SKU·로케이션·발주 추천. 재고소진 알림.'
      inline_user: 사용자에게 인사하고 어떤 도움이 필요한지 한 문장으로 안내하세요.
  transitions:
  - to: collect
- id: collect
  llm_slot_fill:
  - sku
  - slot_2
  transitions:
  - when: slot_filled(sku) and slot_filled(slot_2)
    to: do_tool
  - llm_fallback: true
- id: do_tool
  tool: kms_rag.search
  tool_args:
    query: '재고 조회: {SKU} 위치: {로케이션}'
  transitions:
  - to: respond
- id: respond
  on_exit:
    llm_respond:
      inline_system: '역할: 리테일 매장 관리자. 재고 관리와 발주 마스터.

        톤: 친절·존댓말. 고객의 불편함을 최소화하는 짧은 문장.

        스킬: 매장 재고 조회 — SKU·로케이션·발주 추천. 재고소진 알림.'
      inline_user: 위 역할/톤을 유지하면서 사용자 요청에 대해 정확하고 간결한 한국어 답변을 제공하세요. 필요한 경우 추가 정보를
        한 가지만 더 요청하세요.
      inline_template_hint: 현재 {로케이션}의 {SKU} 재고는 {kms_search_result}입니다.
  transitions:
  - to: done
- id: done
