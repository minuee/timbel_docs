# Industry FAQ Pool

19 카테고리 별 sample FAQ. agent 만들 때 카테고리 선택 시 자동 binding.

활용:
- KMS document 로 등록 → primary_repo_ids 에 포함 → kms_rag.search 가 검색
- 또는 sop_repo_ids 에 포함 → SOP RAG 의 deterministic include

관리자가 *자기 사업장 정보로 수정* 후 KMS 업로드 권장.
