# Install target: /opt/recog/etc/cleanup.env
# Loaded by recog-cleanup.service via EnvironmentFile=.

# Absolute path to the recog-backend runtime directory (the host bind-mount).
RUNTIME_DIR=/home/jwpark/timblo-hynix/recog-backend/runtime

# Optional: override the metric log file. Defaults to $RUNTIME_DIR/cleanup.log.
# CLEANUP_LOG=/var/log/recog-cleanup.log
